# Characters Service (minimal)

Endpoints:
- GET /characters
- POST /characters

Swagger UI: /api-docs

Environment variables:
- PORT
- MONGO_URI (use MongoDB Atlas URI)

To run locally:
1. Install dependencies: `npm install`
2. Set MONGO_URI in .env
3. `npm start`
