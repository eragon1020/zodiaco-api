# Saints Mobile (Expo skeleton)

- Replace <YOUR_CHARACTERS_SERVICE_URL> in App.js with your deployed characters service URL (without trailing slash).
- Install dependencies with `npm install` or `yarn`.
- Start with `npx expo start`.

The app includes a PanGestureHandler example for gestures.
