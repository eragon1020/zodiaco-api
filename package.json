{
  "name": "saints_project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd microservices/characters && npm install && node src/index.js"
  },
  "engines": {
    "node": "18.x"
  }
}
